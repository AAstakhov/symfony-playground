{% extends 'base.html.twig' %}

{% block title %}{{ (author.id) ? 'Edit author' : 'New author' }}{% endblock %}

{% block body %}
    <div>
        <h1>{{ (author.id) ? 'Edit author' : 'New author' }}</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Save"/>
        {{ form_end(form) }}
    </div>
{% endblock %}
