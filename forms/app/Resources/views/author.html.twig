{% extends 'base.html.twig' %}

{% block body %}
<div>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Save"/>
    {{ form_end(form) }}
</div>
{% endblock %}
